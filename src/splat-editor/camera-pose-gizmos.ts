import { Events } from './events';
import { Scene } from './scene';

export class CameraPoseGizmos {
    constructor(events?: Events, scene?: Scene) {
        // Camera pose gizmos - placeholder
    }

    add() {}
    remove() {}
}