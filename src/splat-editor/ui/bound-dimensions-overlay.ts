import { Events } from '../../events';
import { Scene } from '../../scene';

export class BoundDimensionsOverlay {
    constructor(events: Events, scene: Scene, canvasContainer: HTMLElement, annotationContainer: HTMLElement) {}
}