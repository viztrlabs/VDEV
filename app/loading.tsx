import React from 'react';
import ViztrIntroOverlay from '@/components/landing/ViztrIntroOverlay';

export default function Loading() {
  return <ViztrIntroOverlay mode="loading" isLoading={true} />;
}
