# TURN server credentials (used by docker-compose.yml)
# Copy to .env and set a strong password + your VPS public IP.
EXTERNAL_IP=YOUR_VPS_PUBLIC_IP
TURN_USERNAME=viztr
TURN_PASSWORD=change_me_to_a_strong_password
